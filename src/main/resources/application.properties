spring.datasource.url=jdbc:postgresql://localhost:5432/supermarket
spring.datasource.username=s_user
spring.datasource.password=pass
#update to use a created db, update if it exists, create-drop when each time we must redo the creation and inserting (tests)
#Look at ArticleConfig to setup the insertion of data
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
